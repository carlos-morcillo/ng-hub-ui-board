import { InvertColorPipe } from './invert-color.pipe';

describe('InvertColorPipe', () => {
	it('create an instance', () => {
		const pipe = new InvertColorPipe();
		expect(pipe).toBeTruthy();
	});
});
